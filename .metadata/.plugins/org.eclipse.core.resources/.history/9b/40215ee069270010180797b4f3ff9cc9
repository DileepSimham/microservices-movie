spring:
  application:
    name: movie_service
  datasource:
    url: jdbc:postgresql://localhost:5432/reading_excel
    username: postgres
    password: your_password
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080