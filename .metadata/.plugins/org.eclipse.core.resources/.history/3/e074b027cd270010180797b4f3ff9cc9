spring:
  application:
    name: show_service

  profiles:
    active: qa

  config:
    import: "configserver:http://localhost:8071/"

  datasource:
    url: jdbc:postgresql://localhost:5432/movie_service
    username: postgres
    password: Rgukt@1483

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true
  info:
    env:
      enabled: true

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8070/eureka/

info:
  app:
    name: "show-service"
    description: "show service application"
    version: "1.0.0"